import "../../css/texts.css";
import "../../App.css";

const PrimaryHeader = ({ title }) => {
  return(
  <h1 className="header primaryHeader">
    {title}
    </h1>
    
);
};

export default PrimaryHeader;
