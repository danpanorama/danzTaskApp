import '../../css/buttons.css'


const TransparentButton = (props) => {
  return (
    <div className='transparentButton flex-col-center'>
        <p className="text">{props.text}</p>
   
    </div>
  );
};

export default TransparentButton;
